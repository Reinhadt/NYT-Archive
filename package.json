{
  "name": "nyt-archive",
  "version": "1.0.0",
  "description": "Wayback machine to the news",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --files '*.css, *.html, *.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Omar Avalos",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.18.6"
  }
}
